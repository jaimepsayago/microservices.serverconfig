# Discovery Server Access JSH
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka


## HTTP Server
server:
 port: ${PORT:8090}   # HTTP (Tomcat) port
#
#server:
#  port: 8090